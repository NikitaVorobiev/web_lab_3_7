spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/lab_2_7
    username: postgres
    password: "toor"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080  # или любой другой порт, который вы хотите использовать
